document.querySelector(".question-errors").innerHTML = "<%= render 'shared/errors', resource: question %>";

<% if !question.errors.present? %>
  document.querySelector(".question").innerHTML = "<%= j render template: 'questions/_question' %>";
<% end %>